/*
 * macros.h
 *
 *  Created on: Aug 23, 2019
 *      Author: shigurefox
 */

#ifndef MACROS_H_
#define MACROS_H_

#define DEBUG 0
#define SCHOOLBOOK_CUTOFF 5
#define INPUT_BASE 2
#define INPUT_LENGTH 47
#define MODULUS 4591

#define MAX(x, y) ((x) >= (y) ? (x) : (y))
#define MIN(x, y) ((x) <= (y) ? (x) : (y))

extern const int mod_inv[];

#endif /* MACROS_H_ */
